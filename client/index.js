
import React from 'react';
import ReactDOM from 'react-dom';

import App from './containers/App';

// separate this out to introduce redux if we got that complicated

ReactDOM.render(<App />, document.getElementById("root"));
